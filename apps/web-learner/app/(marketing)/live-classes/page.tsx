import { LiveClassesClient } from '@/components/marketing/live-classes-client';

export const metadata = {
    title: 'Lớp Học Trực Tuyến | Torii Nihongo',
};

export default function LiveClassesPage() {
    return <LiveClassesClient />;
}
