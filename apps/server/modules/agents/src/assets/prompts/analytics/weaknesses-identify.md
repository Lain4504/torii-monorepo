Analyze learning patterns for user {{userId}} and identify weaknesses. Look for recurring error patterns in grammar, vocabulary, kanji, particles, verb conjugations, etc. Provide specific areas needing improvement with actionable recommendations. Return as JSON with this exact structure: { "userId": "string", "weaknesses": [{ "area": "string", "severity": number, "examples": ["string"], "recommendations": ["string"] }], "overallAssessment": "string" }