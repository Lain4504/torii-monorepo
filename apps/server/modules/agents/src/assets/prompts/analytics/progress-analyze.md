Track learning progress for user {{userId}}. Activity: {{activity}}{{#if score}}, Score: {{score}}{{/if}}. Analyze the activity, update progress metrics, identify strengths/weaknesses, and provide insights for continued learning. Return as JSON with this exact structure: { "userId": "string", "activity": "string", "score": number (optional), "strengths": ["string"], "weaknesses": ["string"], "insights": ["string"], "nextSteps": ["string"] }