Provide a progress benchmark for user {{userId}} at JLPT {{level}} level. Compare their performance against passing thresholds, identify skill gaps (vocabulary, grammar, reading, listening), and provide readiness assessment with percentage estimates. Also include their next recommended/scheduled test and summary of recent performance. Return as JSON with this exact structure: { "userId": "string", "level": "string", "skillGaps": { "vocabulary": number, "grammar": number, "reading": number, "listening": number }, "readinessPercentage": number, "recommendations": ["string"], "nextScheduledTest": { "date": "string", "type": "string" }, "recentPerformance": { "averageScore": number, "testsTaken": number, "trend": "improving" | "stable" | "declining" } }