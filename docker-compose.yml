services:
  # ========================================
  # Infrastructure Services
  # ========================================

  postgres:
    image: postgres:16-alpine
    container_name: torii-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123456789
      POSTGRES_DB: wajlc
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: [ 'CMD-SHELL', 'pg_isready -U postgres' ]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: torii-redis
    restart: always
    network_mode: "host"
    volumes:
      - redisdata:/data
    healthcheck:
      test: [ 'CMD', 'redis-cli', 'ping' ]
      interval: 10s
      timeout: 5s
      retries: 5

  nats:
    image: nats:2.12-alpine
    container_name: torii-nats
    restart: always
    command:
      - "-DVV"
      - "-config"
      - "/nats-server.conf"
    network_mode: "host"
    volumes:
      - ./nats_server.conf:/nats-server.conf
      - natsdata:/data/jetstream

  livekit:
    image: livekit/livekit-server:v1.9.11
    container_name: torii-livekit
    restart: always
    command: --config /livekit.yaml
    network_mode: "host"
    volumes:
      - ./livekit.yaml:/livekit.yaml
      - livekitdata:/data
      - ./certs:/certs:ro
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7880/healthz"]
      interval: 20s
      timeout: 5s
      retries: 3

  livekit-ingress:
    image: livekit/ingress:v1.4.3
    container_name: torii-livekit-ingress
    restart: always
    command: --config /ingress.yaml
    network_mode: "host"
    volumes:
      - ./ingress.yaml:/ingress.yaml
    depends_on:
      redis:
        condition: service_healthy
      livekit:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8088/"]
      interval: 20s
      timeout: 5s
      retries: 3

  # ========================================
  # Application Services
  # ========================================
  # NOTE: Using pre-built image from Docker Hub to save VPS disk space
  # Image name is configured via DOCKER_USERNAME in .env

  # Uploads: meet service ghi file; gateway phục vụ GET /download/uploadedFile/...
  # Bắt buộc cùng volume — nếu không, user khác không fetch được ảnh whiteboard (chỉ người upload thấy).
  gateway:
    image: ${DOCKER_USERNAME}/torii-backend:latest
    command: npm run prod:gateway
    restart: always
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - NATS_URL=${NATS_URL}
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
    network_mode: "host"
    volumes:
      - ./apps/server/config/config.yaml:/app/apps/server/config/config.yaml
      - uploads_data:/app/apps/server/uploads
    depends_on:
      - nats
      - postgres
      - redis
      - identity
      - meet
      - academy

  identity:
    image: ${DOCKER_USERNAME}/torii-backend:latest
    command: npm run prod:identity
    restart: always
    network_mode: "host"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - NATS_URL=${NATS_URL}
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
    volumes:
      - ./apps/server/config/config.yaml:/app/apps/server/config/config.yaml
    depends_on:
      - nats
      - postgres
      - redis

  academy:
    image: ${DOCKER_USERNAME}/torii-backend:latest
    command: npm run prod:academy
    restart: always
    network_mode: "host"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - NATS_URL=${NATS_URL}
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
    volumes:
      - ./apps/server/config/config.yaml:/app/apps/server/config/config.yaml
    depends_on:
      - nats
      - postgres
      - redis

  meet:
    image: ${DOCKER_USERNAME}/torii-backend:latest
    command: npm run prod:meet
    restart: always
    network_mode: "host"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - NATS_URL=${NATS_URL}
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
    volumes:
      - ./apps/server/config/config.yaml:/app/apps/server/config/config.yaml
      - uploads_data:/app/apps/server/uploads
      - ./storage/trash:/app/trash # Mount trash for janitor
      - ./recording_files/del_backup:/app/recording_backup
    depends_on:
      - nats
      - postgres
      - redis

  agents:
    image: ${DOCKER_USERNAME}/torii-backend:latest
    command: npm run prod:agents
    restart: always
    network_mode: "host"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - NATS_URL=${NATS_URL}
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - REDIS_HOST=${REDIS_HOST}
    volumes:
      - ./apps/server/config/config.yaml:/app/apps/server/config/config.yaml
      - ./apps/server/services/agents/src/assets:/app/apps/server/services/agents/src/assets # Mount prompts
    depends_on:
      - nats
      - redis

  voice-agent:
    build:
      context: .
      dockerfile: apps/voice-agent/Dockerfile
    image: ${DOCKER_USERNAME}/torii-voice-agent:latest
    restart: always
    network_mode: "host"
    env_file:
      - ./apps/voice-agent/.env
    environment:
      - NODE_ENV=production
      - PORT=${VOICE_AGENT_PORT:-8082}
    depends_on:
      - livekit

volumes:
  pgdata:
  redisdata:
  natsdata:
  livekitdata:
  # Dùng chung giữa gateway + meet (uploadPath ./uploads → /app/apps/server/uploads)
  uploads_data:


networks:
  # Separate network for voice-agent with MTU 1400 to fix GCP connectivity.
  # Does NOT affect default network used by LiveKit Meet and other services.
  voice-agent-net:
    driver: bridge
    driver_opts:
      com.docker.network.driver.mtu: 1400
